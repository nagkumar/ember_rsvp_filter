Here Are The List Of Books Fetched From The Server
<ul>
    <h2>Books:</h2>
    <ul>
      {{#each model.books as |book|}}
          <li>{{{book.title}}}</li>
      {{/each}}
    </ul>
    <h2>Filtered Books:</h2>
    <ul>
      {{#each model.filteredBooks as |aFilteredBook|}}
          <li>{{{aFilteredBook.title}}}</li>
      {{/each}}
    </ul>

    <h2>Filtered Sorted Books:</h2>
    <ul>
      {{#each model.filteredBooksSorted as |aFilteredBook|}}
          <li>{{{aFilteredBook.title}}}</li>
      {{/each}}
    </ul>
</ul>
